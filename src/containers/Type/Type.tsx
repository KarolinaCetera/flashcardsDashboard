import React from "react";
import { Button, Input } from "../../components";

export const Type = () => {
	return (
		<div>
			<Input label="Types" />
			<Button buttonText="Add Type" type="submit" />
		</div>
	);
};
